<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:backgroundTint="@android:color/white"
    android:padding="16dp">

    <!-- Konten utama -->
    <LinearLayout
        android:id="@+id/contentLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:layout_above="@id/main_button">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_marginTop="40dp"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <TextView
                android:id="@+id/textView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Laporin!"
                android:textSize="32dp"
                android:fontFamily="@font/poppins_bold" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:weightSum="3">

                <!-- Search -->
                <com.google.android.material.textfield.TextInputLayout
                    android:id="@+id/searchInputLayout"
                    style="@style/CustomTextInputLayout"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1.7"
                    android:layout_marginRight="10dp"
                    android:hint="Cari">

                    <com.google.android.material.textfield.TextInputEditText
                        android:id="@+id/etSearch"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:inputType="text"
                        android:fontFamily="@font/poppins_semibold"
                        android:textColor="@color/textPrimary"
                        android:background="@null" /> <!-- penting: hilangkan background tambahan -->
                </com.google.android.material.textfield.TextInputLayout>


                <!-- Kategori -->
                <com.google.android.material.textfield.TextInputLayout
                    android:id="@+id/inputKategori"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    style="@style/CustomTextInputLayout"
                    android:layout_weight="1.3"
                    android:hint="Kategori"
                    app:boxStrokeColor="@color/outlineColor"
                    app:boxBackgroundColor="@color/none"
                    app:endIconMode="dropdown_menu">

                    <AutoCompleteTextView
                        android:id="@+id/autoKategori"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:inputType="none"
                        android:textColor="@color/textPrimary"
                        android:fontFamily="@font/poppins_semibold"
                        android:padding="12dp" />
                </com.google.android.material.textfield.TextInputLayout>
            </LinearLayout>
        </LinearLayout>

        <!-- RecyclerView -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerView"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:scrollbars="vertical" />

    </LinearLayout>

    <!-- Main Bottom Button -->
    <LinearLayout
        android:id="@+id/main_button"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:orientation="horizontal"
        android:background="@drawable/main_button"
        android:paddingHorizontal="32dp"
        android:paddingVertical="8dp"
        android:layout_marginBottom="40dp"
        android:clipToOutline="true"
        android:weightSum="3"
        android:layout_alignParentBottom="true"
        style="@style/MyRoundedButton">

        <!-- Tombol Pengaduan -->
        <LinearLayout
            android:id="@+id/nav_pengaduan"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:gravity="center"
            android:orientation="vertical"
            android:background="@drawable/bg_nav_selected">

            <ImageView
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:src="@drawable/solar__inbox_outline" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Pengaduan"
                android:textSize="10sp"
                android:textColor="@color/darkGrey"
                android:fontFamily="@font/poppins_semibold"
                android:layout_marginTop="4dp" />
        </LinearLayout>

        <!-- Tombol Tambah (+) -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:gravity="center">

            <ImageButton
                android:id="@+id/nav_add"
                android:layout_width="58dp"
                android:layout_height="58dp"
                android:background="@drawable/bg_add_circle"
                android:src="@drawable/lucide__plus"
                android:contentDescription="Tambah" />
        </LinearLayout>

        <!-- Tombol Profil -->
        <LinearLayout
            android:id="@+id/nav_profil"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:gravity="center"
            android:orientation="vertical"
            android:background="@drawable/bg_nav_selected">

            <ImageView
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:src="@drawable/iconoir__profile_circle" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Profil"
                android:textSize="10sp"
                android:textColor="#A0A8C1"
                android:fontFamily="@font/poppins_semibold"
                android:layout_marginTop="4dp" />
        </LinearLayout>
    </LinearLayout>

</RelativeLayout>
